using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

namespace CISample.DatabaseUpdater
{
    class Program
    {
        static void Main(string[] args)
        {
            // This console app can be run using default settings for the database 
            // or by providing an AppSettings key as a command line argument
            if (args.Length > 0)
            { 

            }
            else
            {

            }
        }
    }
}
